// movies/movie.repository.js
// Placeholder para cuando quieras cachear títulos de TMDB en Postgres.
// Ej: upsert por (external_source, external_id) usando tu tabla cinegestion.movies.
// Por ahora no se usa (passthrough).
export const noop = true;
